import LoginPage from '@/components/Authentication/Login/Login';
import React from 'react';

const page = () => {
    return (
        <div>
            <LoginPage />
        </div>
    );
};

export default page;